# Reanalysis choices — recorded before fresh inference

Status: conditional analysis of recorded main attempts, not accepted confirmatory evidence.
The user requested continuation after the raw audit. The old report is superseded.

- Main input: only the 42 ledger-linked Cheaha shards, checksum-checked; never pool the Colab pilot, cost or repro data. Missing launch pins remain missing. No extra seeds or targets are selected.
- Estimator: micro-average across the fixed fields and three fixed attack seeds; independent D/C persons are resampled, one person draw reused at all k. 10,000 replicates, seed 20240601. Seeds are repeated attacks, not independent people or retraining replicates.
- Zero/full-count intervals: Wilson using the design's conservative target-only n_eff = n_persons * n_fields / (1 + (n_fields-1)*0.5), without multiplying n by seeds. A repeated-attempt ICC=.5 sensitivity uses m=n_fields*3 and n_eff=n_persons*m/(1+(m-1)*.5). These are assumed effective-n approximations, not measured ICC. At any degenerate arm use Newcombe/MOVER for the difference. Otherwise use the prescribed person bootstrap. Report method on every row.
- The primary pointwise 1% H2 claim is unresolved at this n. Preserve the preregistered 9% resolution floor, and also show the actual zero-count Wilson bound (which may be stricter). Apply the joint floor-upper-bound AND nonzero tau-interval criterion at each tolerance. Both descriptive detection and likelihood-ratio optima are reported. Absence of a qualifying point is a sample decision, not proof of impossibility. A negative tau is explicitly signed, not converted into a positive detection claim.
- H1/H3/H4 multiple-testing family remains size FOUR. The design never defines a valid global joint H2 p-value; reserve that slot conservatively with p=1 and label it untestable, rather than using the old floor-only bootstrap event or dropping it. Bootstrap-based p-values are implementation additions: H1 directional sign-tail with finite-simulation correction; H3/H4 centered two-sided bootstrap p, (extreme+1)/(B+1). Holm outputs are conditional sensitivity calculations, not a retrospectively completed confirmatory testing contract.
- H4: CONTROL-only log-log interval-censored Weibull AFT, gamma vs 1, with 10,000 person bootstrap refits. Weibull was not fixed by the design, so it is an explicitly disclosed inherited working distribution; inspect a log-normal alternative. k_min is the first capacity with any hit across the three fixed seeds, matching the original definition; per-seed thresholds are shown as sensitivity, never substituted for the primary definition. Do not call a CI containing 1 support/equivalence. Beta is not an interpretable constant bits/token rate if proportionality fails.
- The Weibull interval likelihood may be optimized directly for speed only after numerical agreement with lifelines==0.30.0 on the full data and selected bootstrap resamples. Record this solver deviation and every failed fit. Use the same control-person draws as the curve bootstrap. Secondary level-normal interval-censored Tobit uses the same draws. Complete-case linear fits and direct median H/k brackets are diagnostics only.
- H5: keep every tied maximizer; lower/upper percentile interval is the envelope of earliest/latest maximizers. Report first-argmax sensitivity explicitly. Include the log-k quadratic coefficient and bootstrap interval; no statistical 'flatness' threshold was fixed, so non-detection never becomes proof of flatness. Always label exploratory and underpowered.
- Final completeness check: also report the protocol's isotonic summary without replacing observed rates. The exploratory quadratic coefficient's one-sided p uses a null-centered left tail with add-one correction, the same disclosed bootstrap convention; the independent review covers the main numerical comparisons and does not claim an independent recomputation of this added p. The ideal zero-hit person-count calculation for a 1% Wilson bound is a precision illustration, not a new powered design.
- Recover the actual attack-subset marginal balance from raw k>=1 targets and recovered registry. The two original E17 SMD variants can be recomputed for the recovered Colab matching record, labelled by that provenance; they do not prove the missing Cheaha matching files are identical. Do not silently substitute one for the other.
- NLL/AUC and TPR-at-observed-FPR summaries are exploratory. No certified DP epsilon is inferred from an uncorrected point TPR/FPR ratio. The proposed per-target H(t)/log2(V) deterministic lower bound in the old report is withdrawn; reference-model self-information is not format min-entropy.
- Timing: sum recorded attack-call wallclock by arm as observed attack elapsed hours; keep scheduler accelerator-hours, failed-job cost and full study budget consumption unknown.
